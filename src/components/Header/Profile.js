import classes from "./Profile.module.css";

const Profile = () => {
  return (
    <>
      <a className={classes.profile} href="#">
        Logout
      </a>
    </>
  );
};

export default Profile;
